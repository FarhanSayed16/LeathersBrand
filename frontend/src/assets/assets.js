import razorpay_logo from './razorpay_logo.png'
import bin_icon from './bin_icon.png'

/** Slim icon map — product PNGs live in the API / brand assets, not the JS bundle. */
export const assets = {
  razorpay_logo,
  bin_icon,
}
