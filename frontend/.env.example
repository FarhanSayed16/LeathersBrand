# Frontend (Vite) — copy to .env / .env.production
# No trailing slash on the backend URL
# Match PORT from backend/.env (template default 4000; some local setups use 5000)

VITE_BACKEND_URL=http://localhost:5000
VITE_RAZORPAY_KEY_ID=rzp_test_xxx
